{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node -e \"const { execSync } = require('child_process'); execSync('pnpm docs', { cwd: '../..' }); execSync('pnpm --filter @headless-media/ui-react build-storybook', { cwd: '../..' }); fs.cpSync('../../docs/api', 'public/docs', { recursive: true }); fs.cpSync('../../packages/media-ui-react/storybook-static', 'public/storybook', { recursive: true });\"",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@headless-media/core": "workspace:*",
    "@headless-media/react": "workspace:*",
    "@headless-media/ui-react": "workspace:*",
    "lucide-react": "^1.30.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "react-router-dom": "^6.26.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.4.0",
    "vite": "^6.0.0"
  }
}
